import React from "react";

export default function Settings() {
    return (
        <section>
            <h2 className="text-3xl font-semibold mb-4">Settings</h2>
            <p className="text-gray-600">This page would contain application settings and preferences.</p>
        </section>
    );
}
